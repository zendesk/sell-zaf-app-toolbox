import { ClientContextResponse } from '../types';
interface Options {
    skip?: boolean;
}
export declare function useClientContext(dependencies?: any[], options?: Options): ClientContextResponse;
export default useClientContext;
